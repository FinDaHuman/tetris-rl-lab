# Session Log: 2026-07-07

Purpose of this session:

- Review the project and the overnight run.
- Decide what to do next to increase clear lines across all tracks.
- Update notes so future sessions can resume cleanly.
- Preserve enough detail for a later project report.

## Project Understanding

The repo has four tracks:

- Track 1: pure RL on ALE using `agents/ale/pure_rl_ale_agent.py`.
- Track 2: tool-assisted ALE high score using `agents/ale/ale_tetris_agent.py`
  and root wrapper `ale_tetris_agent.py`.
- Track 3: pure RL on the custom Gymnasium environment using
  `agents/custom/pure_rl_custom_agent.py`.
- Track 4: tool-assisted custom high score using
  `agents/custom/tetris_custom_agent.py`.

Pure-RL tracks must not use board decoding, placement enumeration, search,
state cloning, demonstrations, or heuristics. Tool-assisted tracks may use those
techniques.

## Reviewed Overnight Track 1 Run

Input evidence:

- `note.md`
- `Windows PowerShell.txt`
- `artifacts/ale_pure_rl/meta.json`
- `runs/ale_pure_rl/eval/evaluations.npz`

Findings:

- The overnight Track 1 command trained ALE pure-RL PPO for 10M timesteps with
  sticky actions.
- Training completed and saved `artifacts/ale_pure_rl/ppo_ale_pure.zip`.
- Final evaluation printed 25 rows, all with `estimated_lines: 0`.
- Average eval length from the printed rows was about 487 steps.
- The eval command crashed only while writing JSON because `episode` info
  contained NumPy scalars. The printed results still prove the model cleared 0
  lines.
- Eval callback history had only two isolated nonzero reward checks:
  - 4.25M timesteps: one of five eval episodes got reward 1.
  - 9.5M timesteps: one of five eval episodes got reward 1.
  - These did not persist.
- The callback `best_model.zip` was evaluated across 25 fresh seeds and also
  cleared 0 lines.

Conclusion:

- Repeating the exact Track 1 10M sticky PPO run is not the best next step.

## Baseline Checks Run

Command:

```powershell
python -m pytest
```

Result:

- 6 tests passed.
- Track-boundary AST checks passed.

Additional checks:

- Track 2 stable ALE model reproduced 37 lines on seeds 0, 1, and 2.
- Track 3 current pure-RL custom checkpoint evaluated over 10 deterministic
  episodes and cleared 0 lines.
- Track 4 shallow preview evaluation showed high seed variability, but the
  queue lookahead path was much stronger.

## Notes Updated

Updated `note.md` into an overnight clear-line run plan.

Added `NEXT_STEPS.md` with:

- Current Track 4 result summary.
- 500-piece validation command.
- 500-piece warm-start training command.
- 500-piece evaluation command.
- Promotion rule for not overwriting `artifacts/custom_best` too early.

## Track 4 Progress

The user ran:

```powershell
python agents/custom/tetris_custom_agent.py evaluate --weights runs/overnight_lines_20260707/custom_tool_queue/best_weights.npy --episodes 10 --max-pieces 200 --lookahead-depth 2 --lookahead-candidates 4 --future-source queue --out runs/overnight_lines_20260707/custom_tool_queue/evaluation_200.json
```

Result:

| Seed | Score | Lines | Pieces |
|------|------:|------:|-------:|
| 0 | 36000 | 79 | 200 |
| 1 | 36100 | 78 | 200 |
| 2 | 33500 | 76 | 200 |
| 3 | 35700 | 79 | 200 |
| 4 | 36600 | 79 | 200 |
| 5 | 37800 | 79 | 200 |
| 6 | 35700 | 78 | 200 |
| 7 | 39000 | 79 | 200 |
| 8 | 36300 | 78 | 200 |
| 9 | 36300 | 78 | 200 |

Summary:

- Mean score: 36300.0
- Max score: 39000.0
- Mean lines: 78.3
- Max lines: 79
- Runtime: 313.7 seconds
- Output file:
  `runs/overnight_lines_20260707/custom_tool_queue/evaluation_200.json`

This is a strong Track 4 improvement. At a 200-piece cap, 80 lines is the
practical ceiling if every four pieces clear one line, so 78.3 mean lines is
near saturation.

Optimization history:

- `runs/overnight_lines_20260707/custom_tool_queue/history.json`
- Best fitness improved from 113986.67 at generation 1 to 116686.67 at
  generation 12.

Learned weights:

- `runs/overnight_lines_20260707/custom_tool_queue/best_weights.npy`
- Metadata saved in `runs/overnight_lines_20260707/custom_tool_queue/meta.json`.

## Current Recommendation

Do not keep optimizing Track 4 at 200 pieces. Move to 500 pieces.

Next command:

```powershell
python agents/custom/tetris_custom_agent.py evaluate --weights runs/overnight_lines_20260707/custom_tool_queue/best_weights.npy --episodes 5 --max-pieces 500 --lookahead-depth 2 --lookahead-candidates 4 --future-source queue --out runs/overnight_lines_20260707/custom_tool_queue/evaluation_500.json
```

If that looks good, train:

```powershell
python agents/custom/tetris_custom_agent.py train --outdir runs/overnight_lines_20260707/custom_tool_queue_500 --warm-start runs/overnight_lines_20260707/custom_tool_queue/best_weights.npy --generations 8 --population 16 --rollouts 2 --max-pieces 500 --seed 17 --lookahead-depth 2 --lookahead-candidates 4 --future-source queue
```

Then evaluate:

```powershell
python agents/custom/tetris_custom_agent.py evaluate --weights runs/overnight_lines_20260707/custom_tool_queue_500/best_weights.npy --episodes 10 --max-pieces 500 --lookahead-depth 2 --lookahead-candidates 4 --future-source queue --out runs/overnight_lines_20260707/custom_tool_queue_500/evaluation_500.json
```

Promotion rule:

- Do not overwrite `artifacts/custom_best` yet.
- Promote only after the 500-piece result clearly beats the current saved model
  on mean lines or max lines.

## Report Reminder

The final report should explain:

- What was built: four separated Tetris agent tracks and a custom Tetris engine.
- How it was built: pure RL with PPO, tool-assisted placement/search/CEM,
  ALE integration, custom Gymnasium environment.
- How agents performed:
  - Track 1: 10M ALE PPO, 0 lines on final 25-episode eval.
  - Track 2: stable 37 ALE lines on checked seeds.
  - Track 3: current custom pure-RL checkpoint, 0 lines.
  - Track 4: new queue-lookahead custom model, mean 78.3 lines at 200 pieces.
- Why results differ: sparse raw-pixel ALE learning is hard; custom
  tool-assisted queue lookahead gets direct access to placement search and is
  therefore much stronger.
