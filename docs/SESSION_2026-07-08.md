# Session Log: 2026-07-08

Purpose of this session:

- Deep-dive review of all four tracks: code, algorithms, and training plans.
- Implement best-practice fixes for the issues found. No overnight training
  was run; the runnable plan is in `TRAINING_PLAN.md`.

## Issues Found And Fixed

### 1. Track 3 reward hacking (root cause of 0 lines)

The engine's score-mode reward pays +1 per soft-drop cell and +2 per hard-drop
cell, so a policy earns ~30+ points per piece just by dropping fast, while a
single line clear (100 points) requires placement skill the policy never
stumbles into. PPO therefore converged to fast dropping with 0 lines.

Fix: `TetrisScoreEnv` now supports `reward_mode="lines"` (new default in the
Track 3 CLI): `line_reward * cleared^2` per lock, `piece_reward` per placed
piece, `-top_out_penalty` on termination, and no drop points. The per-piece
term is survival-aligned because a board tops out after ~50 pieces unless
lines clear. Legacy behavior remains as `reward_mode="score"`.
Files: `packages/tetris_env/tetris_env/gym_env.py`,
`agents/custom/pure_rl_custom_agent.py`. Tests: `tests/test_reward_modes.py`.

### 2. Track 4 planner performance (~5x speedup)

Profiling showed ~80% of planning time in `enumerate_placements` (full game
clone plus cell-by-cell drop per candidate: ~394k `_try_shift` calls per 30
pieces) and in `np.pad` inside the feature functions. Also, symmetric
rotations (O all four; I/S/Z opposite pairs) were enumerated as duplicate
candidates, wasting compute and beam slots.

Fix: `enumerate_placements` rewritten as a numpy column-tops drop simulation
with an exact cell-wise fallback for covered-pocket spawns; symmetric
footprints deduped; features vectorized and fused.
Measured: 2 episodes x 60 pieces went 19.0s -> 3.6s; a 500-piece episode now
takes ~15s (was ~78s). An oracle test compares the new enumeration against
the original clone-based algorithm across random rollout states and requires
identical placement sets (modulo duplicates).
Files: `packages/tetris_env/tetris_env/engine.py`,
`packages/tetris_env/tetris_env/features.py`. Tests: `tests/test_features.py`,
`tests/test_engine_placements.py`.

Side effect to remember: with duplicates gone, beam search sees more distinct
candidates, so re-evaluating the same weights can score slightly differently
(spot check: mean 3450 -> 3550, lines 21.0 -> 21.5). Post-change evaluations
should not be mixed with pre-change numbers in the report.

### 3. Track 4 CEM lucky-seed promotion

`best_weights.npy` was promoted on per-generation training seeds, which change
every generation, so cross-generation comparisons were not apples-to-apples.
Fix: promotion now uses a fixed held-out seed set (`--holdout-rollouts`,
default 3), the warm-start is scored on it first as the baseline, and
`history.json` records `gen_best_train` / `gen_best_holdout` / `best_holdout`.
File: `agents/custom/tetris_custom_agent.py`.

### 4. Track 1 eval JSON crash

`evaluate --out` crashed serializing NumPy scalars in ALE episode info. Fixed
with a `default=` converter in `json.dumps`.
File: `agents/ale/pure_rl_ale_agent.py`.

## Review Conclusions (unchanged from analysis)

- Track 1 pure RL on ALE is a near-unwinnable task for vanilla PPO at laptop
  compute; treat as a documented negative result. `TRAINING_PLAN.md` includes
  one optional, capped 3M non-sticky attempt for the report.
- Track 2 stays parked at the stable 37-line result.
- Track 3 is the best pure-RL opportunity now that the reward is fixed.
- Track 4 moves to the 500-piece cap (ceiling 200 lines); default weights
  already reach ~192 lines there, so gains are in the last lines and in mean
  consistency.

## Verification Run This Session

- `python -m pytest`: 14 passed (5 new tests added this session).
- Track 3 smoke, plus a 3k-timestep PPO train/evaluate round trip in lines
  mode (VecNormalize save/load and eval JSON confirmed working).
- Track 4 tiny CEM train (2 generations) confirming holdout promotion logic.
- JSON converter checked against ALE-style NumPy episode payloads.

## Next

Run the three nights in `TRAINING_PLAN.md`, in order, with the transcripts
enabled. Promotion rules are in that file and mirrored in `NEXT_STEPS.md`.

---

# Addendum: Night 1 review (same day, evening)

Night 1 of the plan was run by the user; review results:

## Track 4 promoted on score

Old vs new weights, both at 10 episodes, seeds 0-9, 500 pieces, queue
lookahead: mean score 213,780 → 215,530 (new better on 6/10 seeds), mean
lines 198.3 → 198.1 (tie; the 500-piece ceiling is 200 and both sit against
it). CEM holdout fitness rose 412,150 → 420,117 across 12 generations. The
original gate said "promote on mean lines", but lines are saturated and
Track 4's objective is score, so promotion was decided on mean score. The
extra 10-episode eval of the old weights (for a fair same-seed comparison)
is `runs/plan_20260708/track4_eval500_current_10ep.json`. Promoted files:
`best_weights.npy`, `evaluation_500.json`, `history.json`, `meta.json` in
`artifacts/custom_best/`.

## Track 3 pilot: ~3,900 fps; Night 2 resized 5M → 50M

The 100k-step pilot finished in 38 s (~3,900 fps steady on 8 dummy envs).
Learning signal is healthy: ep_len_mean 65 → 198, ep_rew_mean -6.77 → -4.46
over 100k steps (0 lines yet, expected). PPO diagnostics note:
clip_fraction ~0.4 / approx_kl ~0.05 is hot; a lower learning rate is a
future lever if 50M looks unstable.

## Bug fix: env replayed one piece sequence per env, every episode

The pilot's eval callback logged 5 *identical* episodes, which exposed a
seeding bug in `TetrisScoreEnv.reset`: unseeded resets rebuilt
`TetrisGame(seed=self._seed)`, so each env replayed the same piece sequence
for its whole life (training saw only `n_envs` distinct sequences; eval-best
selection was based on a single sequence). Fix: the constructor seed now
seeds the env RNG once, and every unseeded reset draws a fresh game seed
from that stream — episodes differ, runs stay reproducible per env seed,
and explicit `reset(seed=...)` is still deterministic. Three regression
tests added in `tests/test_custom_env.py`; suite now 17 passed.

Consequences: the standalone `evaluate` subcommand always seeded each
episode explicitly, so 0-line historical Track 3 evals stay valid. Piece
sequences for a given seed changed (game seed is now RNG-derived), so
seed-for-seed comparisons across the fix are not meaningful. Track 4 is
unaffected (it drives `TetrisGame` directly with explicit seeds).

## Logging gotcha

`Start-Transcript` did not capture Python stdout, so the Night 1 transcript
was nearly empty; the user's saved console window
(`runs/plan_20260708/night1_console_full.txt`) is the real evidence. Do the
same for later nights.
